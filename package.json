{
  "name": "svelte-puzzle-8",
  "version": "0.1.0",
  "description": "Runner for Svelte puzzle 8",
  "scripts": {
    "setup-project": "cd ./frontend && npm install && cd ../ && python -m venv venv && venv\\Scripts\\activate && pip install -r requirements.txt",
    "frontend-build": "cd ./frontend && npm run build",
    "frontend-dev": "cd ./frontend && npm run dev",
    "backend-dev": "venv\\Scripts\\activate && python -m backend.main --dev",
    "backend-run": "venv\\Scripts\\activate && python -m backend.main",
    "build-and-run": "npm run frontend-build && npm run backend-run"
  },
  "author": "Stefik_O",
  "license": "MIT"
}
